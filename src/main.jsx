import React from "react";
import { createRoot } from "react-dom/client";
import ApexFlow from "./ApexFlow.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ApexFlow />
  </React.StrictMode>
);
